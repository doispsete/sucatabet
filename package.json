{
  "name": "sucatabet-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "cross-env NODE_OPTIONS=--max-old-space-size=8192 concurrently \"npm run start:dev -w api\" \"cross-env TURBOPACK=0 npm run dev -w web -- -p 3000\" --names api,web",
    "api:build": "npm run build -w api",
    "web:build": "npm run build -w web",
    "prisma:generate": "npm run prisma:generate -w api",
    "build:prod": "npm run clean && npm run prisma:generate && npm run api:build && npm run web:build",
    "clean": "npx rimraf apps/api/dist apps/web/.next"
  },
  "dependencies": {
    "@prisma/client": "7.6.0",
    "prisma": "7.6.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^10.1.0",
    "rimraf": "^6.0.1"
  }
}
